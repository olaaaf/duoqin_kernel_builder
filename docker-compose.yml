services:
  android:
    build:
      context: .
      dockerfile: android.dockerfile

    volumes:
      - kernel:/kernel
      - ~/Desktop/output:/output:Z

    command: ["bash"]
    stdin_open: true
    tty: true

volumes:
  kernel: {}
